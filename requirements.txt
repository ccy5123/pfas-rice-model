# PFAS-rice model + Streamlit dashboard -- the full stack the app needs.
# This single file is what Streamlit Community Cloud installs (deploy app.py).
# core model:
numpy>=1.23
scipy>=1.10
matplotlib>=3.6
# dashboard (app.py / `streamlit run app.py`):
streamlit>=1.30
plotly>=5.18
pandas>=1.5
# SMILES (structure) compound input mode in the app (src/pfas_structure.py):
rdkit>=2023.9
# live "Run HYDRUS-1D" mode: drives the engine (built in-app via gfortran; see packages.txt):
phydrus>=0.2
# EPA CompTox (CTX) property lookup in the app (src/chem_lookup.py). The lookup is
# OPTIONAL -- it needs a free EPA API key and outbound network, and degrades to
# manual entry without either -- but requests ships anyway since it is tiny and
# Streamlit already pulls it in transitively:
requests>=2.28
